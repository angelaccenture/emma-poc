// The "landing" template is CSS-only (auto-alternating band backgrounds and
// vertical rhythm). No JS behavior needed — kept as a no-op so the template
// loader has a module to import.
export default async function init() {
  // Intentionally empty.
}
